# T-110 — VEA-G3 Phase A 設計固定

- blocked_by: T-020, DEC-004
- status: BLOCKED

## 目的
Phase A の固定値を、結果を見る前に確定する。

## 前提
`control/decisions.md` の DEC-004 内訳表に `PENDING` が 1 件も無いこと。
SRC-03 の候補値（片側 α=0.05、検出力 0.80、τ=0.10、n_pair=153、生成 184）は
**候補** であり、採用の可否自体が DEC-004 の一部である。

## 絶対禁止（SRC-02）
結果を閲覧した後に、データ・モデル・プロンプト・閾値・N・seed・除外・検定・停止規則・
コスト・判定規則を変更しない。変更が必要になった場合は新規の prereg として起こす。

## 完了条件
- 固定値表が全項目埋め。
- 統計判定は決定論スクリプトで実装されている（LLM に検定選択をさせない。SRC-02 SPEC§39, §73）。
- 判定語彙は S/E/U/N/O のみ。`U` を `PASS`/`FAIL` に変換しない。
